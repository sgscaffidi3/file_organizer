# ==============================================================================
# File: requirements.txt
# Dependencies required to run the File Organizer project.
# ==============================================================================

# Core Utility Dependencies
# Tqdm is highly recommended for adding progress bars to scanning and processing
# phases for a better user experience on large datasets.
tqdm

# Metadata Extraction Dependencies (Functional Requirement F04)
# Pillow (PIL) is the standard library for processing image metadata (EXIF, IPTC,
# and size/dimensions) and fulfilling F04 for image files.
Pillow

# Hachoir is used for extracting metadata from video and binary files (F04).
hachoir